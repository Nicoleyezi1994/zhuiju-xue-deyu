# 如何用 AI 助手生成一集学习数据

这份文档是给你（或者你请来帮忙的 AI 编程助手）看的：把 README 里"生成你自己的数据文件"那五步展开成一套具体做法，附带一些做多了才会踩到的坑。跟着这套流程走，第一次就能做出比"能跑起来"更靠谱一些的数据文件——不用等你自己回头一条条纠正。

这份文档不涉及任何具体的剧、人物或台词，纯粹讲方法和数据格式。

## 前提

你需要：一份目标剧集的 `.srt` 字幕文件（原语言，比如德语），以及（可选，但强烈建议）一份对应的中文翻译来源——可以是你自己边看边记的理解，也可以让 AI 助手直接翻译。

下面所有命令都用 `python3` 举例（macOS/Linux 通常都是这个命令名）。**如果你在 Windows 上、且是从 python.org 官方安装包装的 Python**，命令通常叫 `python` 而不是 `python3`（装的时候如果勾选了"Add python.exe to PATH"，直接在终端/PowerShell 里输入 `python` 就行）；如果不确定，两个都试一下，哪个能跑就用哪个。

## 第一步：解析 srt

```bash
python3 tools/parse_srt.py 你的字幕.srt cues_output.json
```

会得到一份按时间戳排好的 cue 列表（每条含 idx、start/end 毫秒、说话人、正文、是否纯音效）。这一步不用改代码。

## 第二步：核实真实的剧集标题

**不要凭"这一集反复出现的关键词"去猜标题**——猜错过好几次。做法：搜索这部剧的官方剧集列表页面（比如维基百科、IMDb、或者剧集数据库网站的 Episodenguide），核对真实标题拼写和这一集对应的季/集数。

## 第三步：逐句翻译（最容易出错的一步）

**教训**：翻译台词时，如果按"顺序列表"来写（一行对一行），只要有一处因为重复短句、空行、或者顺序理解错位而多算/少算了一行，后面所有翻译就会整体错位，而且很难发现——因为每一句读起来都通顺，只是对应错了原文。

**正确做法**：

1. 把所有"非纯音效、非空"的对话行连同各自的 idx 列出来。
2. 写一个字典/映射，**key 必须是 srt 里那条 cue 的 idx（整数），value 是译文**——不要写成顺序列表、不要假设行号会自然对齐。
3. 翻译完成后跑一次对齐校验：
   - "所有对话类 cue 的 idx 集合" 减去 "翻译里的 key 集合" 应该是空集（没有漏翻）。
   - "翻译里的 key 集合" 减去 "所有对话类 cue 的 idx 集合" 也应该是空集（没有翻到不存在的行/多余的 key）。
   两边都为空，才说明真的对齐了，这一步不能省。
4. **只做上面的对齐校验还不够**：对齐只能保证"每句都有翻译"，不能保证"翻译内容真的对应这一句"。特别是只有 1-2 个词的超短句（比如独立成句的一个感叹词、一个"是"或"不是"），最容易在翻译时被"按整段语义意译分配"到相邻的句子上，结果内容整体错位了一两句却查不出来（因为 idx 集合本身没有变化）。做法：翻译完成后，把所有 ≤2 个词的超短句单独列出来，逐条对照原文核对翻译是不是这一句本身的意思，而不是前后句的内容。
5. 翻不出的双关语/俗语不用硬翻进译文本身，可以在对应的 vocab 或 grammar 条目的 note 里另外解释。

## 第四步：挑选生词（vocab）

**难度校准是两个独立的筛选条件，缺一不可**：

- **条件一：日常场景可迁移**。不要因为某个词是剧情高频词就选它——如果这个词只在这部剧特定的行业/场景背景下才用得上（比如某个很窄的专业术语），换到日常对话里几乎用不上，就不值得占用一个生词名额，除非它本身也是日常常见词、只是恰好在剧情里出现得多。
- **条件二：真正达到目标难度，不能只是"看起来常用"**。这里有个反直觉的坑：**"日常常用"不等于"简单"**。有些词虽然在剧情里反复出现，但其实是初学者早就学过的基础词（比如"觉得""大概""没关系"这类），选它们只是在凑数，起不到教学作用。同时还有另一个方向的坑：**国际同源词（一看就懂的外来词/学术词）哪怕某些词表标注成中高级，也可能"太简单"**——比如一个词的拼写和意思跟英语几乎一模一样，学习者不用学就能猜对，这种词即使"技术上"够级别，也没有真正的教学价值。判断标准不是词表等级标签，而是：一个已经掌握基础语法和常用词汇的学习者，看到这个词/搭配/例句时，会不会因为不知道某个具体的义项、搭配习惯或语域，而真的卡住？如果答案是"不会，看一眼就懂"，那就还是太简单。

真正该优先选的，是同时满足"日常任何场景都能迁移使用"+"确实需要专门学一下才会用对"这两个条件的词——常见的例子包括：一词多义词（表面意思你认识，但某个引申义/固定搭配你未必知道）、构词逻辑值得展开讲的复合词、语域/语气微妙的副词、真正常用的习语（而不是拼凑出来的生僻俏皮话）。

**跨集去重（做系列剧才需要，单集示例不涉及）**：如果你在准备同一部剧的多集数据，先建一份 `tools/used_vocab_grammar.json`（可以从 `tools/used_vocab_grammar.example.json`复制一份开始），记录已经精讲过的生词（含所有词形变化）和语法点主题。挑到候选生词后用：

```bash
python3 tools/check_dup.py 候选词1 候选词2 ...
```

命中说明这个词根已经在前面某一集精讲过，除非这次出现了全新的、值得单独讲的搭配或语法用法（并且要在 note 里写清楚"新在哪里"），否则不要重复选它。目标是同一部剧的全系列里，同一个词根只精讲一次。

**每个生词条目需要的字段**：`id`、`term`、`pos`(词性)、`level`(CEFR等级)、`meaning_zh`(中文释义)、`example_de`(**必须是某条真实 cue 里原样出现的句子，不要自己编**)、`example_zh`(对应译文)、`note`(词源/搭配/文化背景等)、`match`(数组，列出这个词在全集里可能出现的所有词形变化，用于播放器自动高亮匹配)。

**做完生词表后，必须跑一次高亮校验**：

```bash
python3 tools/check_highlight.py 你的episode-id
```

"`example_de` 的文本出现在某条 cue 里"不等于"播放器真的会高亮这个词"——播放器是按单词 token（用 `[A-Za-zÀ-ÖØ-öø-ÿß']+` 这样的正则切词）逐个去匹配 `match` 数组里的词形的，所以下面几种情况文本检查能通过、但高亮实际不会触发：连字符复合词（会被切成两个 token）、可分动词被拆开使用（出现的 token 是变位形式，不是词典原形）、`match` 里写的多词短语中间被别的词插了进去、没有分隔符的长复合词（整个是一个 token，如果 `match` 里只写了它的一部分就匹配不到）。脚本会列出有问题的条目，针对每一条要么把实际会出现的 token/短语加进 `match` 数组，要么换一个真正包含该词的 cue 作为例句。

**Python 字符串里的引号坑**：如果 `note`/`explain_zh` 这类字段用英文双引号 `"..."` 包裹整段中文说明，说明文字内部又想用英文引号标注术语，会导致字符串提前闭合、Python 报语法错误。统一改用中文全角引号「」来标注术语，就能完全避开这个坑。

## 第五步：语法点（grammar）

同样优先选"换成任何一段无关的日常对话例句、这个语法点依然成立、依然有用"的结构，而不是只在这部剧特定情境（比如某个专业领域的固定说法）里才用得上的知识点。

如果是系列剧，同样先查 `used_vocab_grammar.json` 里已经覆盖过的语法主题——像被动语态、虚拟式、反身动词、关系从句这类几乎每集都会自然出现的基础结构，如果之前已经完整讲过、这次只是换个例句重复同一个角度，就不要再单独列为本集的语法点；除非这次出现了一个之前没覆盖过的具体子用法，且能在讲解里写清楚这次的角度跟之前有什么不同。

每条包含：`id`、`title`、`tag`(简短分类标签)、`cue_idx`(对应的 cue 编号)、`explain_zh`、`example_de`/`example_zh`(**同样必须来自真实 cue，且 `example_de` 要跟 `cue_idx` 指向的那条 cue 文本完全一致**——务必手动核对一遍，选错 `cue_idx` 会导致播放器暂停在错误的时间点却显示这个语法讲解，不容易第一时间发现)。

## 第六步：总结、关键片段、练习题

- **`summary.key_phrases`（6-8 条本集最值得记住的短语）**：必须是真正有语言学习价值的固定短语/习语/句式，比如某个"疑问词+助词"构成的感叹句型，或者一个地道的习语——而不是"剧情上很关键但语言上很平淡"的句子（比如一句简单的"我累了"或者"我们得走了"，这种句子哪怕在剧情推进上很重要，也没有固定搭配/句式价值，不该占用 key_phrases 的名额；剧情交代放在 `synopsis_zh` 或 `keyScenes` 的 `title` 里就够了）。另外，**`key_phrases` 里的每一条必须是真正的多词短语，不能是一个孤零零的单词**——哪怕这个词已经在 `vocab` 里精讲过、哪怕它本身很地道也不行；一个短语同时出现在 `vocab` 和 `key_phrases` 里没问题，只要它本身是多词搭配。
- **`keyScenes`（6-8 段值得回放的片段）**：`start`/`end` 必须是真实 cue 的时间戳（从解析出来的 cues 里查，不要自己估算）。**`note_zh` 不能只是剧情摘要**，要点出"这段值得回放的语言学习理由"——语速快慢、正式/口语register、信息密度、适合精听还是跟读、适合哪类词汇/句型的学习者。`title` 已经承担了"讲了什么剧情"的功能，`note_zh` 不需要再复述剧情细节，控制在 25-40 字左右，几乎全部篇幅放在语言特征上。
- **`quiz`（8-10 道题）**：只留 1-2 道纯剧情理解题，其余大多数应该针对本集选的生词/语法点做考查和运用，而不是变成一个剧情问答测试。
  - `type="mc"`（选择题）作为主力题型：`q`、`options`(4个选项)、`answer`(正确选项下标)、`expl`。
  - `type="fill"`（填空题）留 2 道左右就够，字段 `q`(用 `___` 表示空)、`answer_text`、`expl`。播放器按"用户输入是否包含这个答案字符串"判分（不区分大小写）。
    - **`q` 必须给出含义提示，不能只标注词性/语法类别**：只写"这是个动词"完全不够，学习者看到空格根本不知道该填哪个词——哪怕这个词是本集精讲过的生词也一样。要在括号里给出目标词/搭配的含义释义（例如：`Ergänzen Sie das Verb (= für etwas verantwortlich sein): ...`）。固定搭配/俗语类的填空同样要给含义提示，光给出搭配骨架（比如"in den ___ X"这种残缺短语）是不够的，必须额外说明这个搭配整体的意思。唯一不需要额外含义提示的情况是**纯语法变形题**——答案完全由题目给出的语法规则/转换本身推导得出，不依赖记住某个具体的词或俗语（比如被动语态的助动词变位）。
    - **`answer_text` 里如果用"/"分隔多个字符串，表示的是"任意一个都算对"（OR逻辑），不是"必须都填对"（AND逻辑）**：如果一道题的空格设计成需要同时填两个不同的词（比如一个句子里挖了两个空），用"/"来分隔这两个词会导致用户只填对其中一个就被误判为满分——这不是想要的效果。遇到这种情况，要么只留一个空、把另一个词直接写进题干里，要么拆成两道单独的填空题。

## 第七步（系列剧）：追加进跨集追踪表

本集数据定稿后，把新增的 vocab 和 grammar 条目追加进 `tools/used_vocab_grammar.json`（只追加，不要覆盖已有条目），下一集做的时候才能查到最新状态。

## 第八步：本地测试

用任意静态文件服务器（或直接双击 `index.html`）打开播放器，检查：下拉框里出现新加的这一集且标题正确；切换过去后生词/语法/总结/关键片段/练习题都能正常显示；挑 1-2 个生词点开确认高亮和释义卡片正常；练习题填空/选择都能正确判分。
